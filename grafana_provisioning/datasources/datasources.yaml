apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: $INFLUX_DB
    user: $INFLUX_USER
    secureJsonData:
      password: $INFLUX_PASS
    url: http://influxdb:$INFLUX_DBPORT
    isDefault: true
    editable: true
